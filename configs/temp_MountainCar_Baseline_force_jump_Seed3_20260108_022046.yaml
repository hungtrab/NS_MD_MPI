# MountainCar Baseline - Jump Force Drift - PPO
env_id: "MountainCar-v0"

env:
  parameter: "force"
  drift_type: "jump"
  magnitude: 0.0005
  period: 5000

wandb:
  project: "NS-MDP-Experiments"
  entity: null
  tags: ["mountaincar", "baseline", "jump", "force"]
  mode: "online"

train:
  algorithm: "PPO"
  learning_rate: 0.0002
  n_steps: 2048
  batch_size: 64
  gamma: 0.99
  total_timesteps: 5000000
  seed: 3

adaptive:
  enabled: false

nsmdmpi:
  enabled: false

paths:
  log_dir: "logs/"
  model_dir: "models/"
  video_dir: "videos/"
